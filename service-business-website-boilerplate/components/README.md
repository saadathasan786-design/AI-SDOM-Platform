# Components

Reusable, accessible UI components for the service-business website baseline.

Components must receive project-specific content through explicit inputs and must not embed fabricated business facts.