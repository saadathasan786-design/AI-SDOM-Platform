<?php
/**
 * Fallback template — block themes are primarily driven by /templates/*.html,
 * but WordPress still requires index.php to exist for the theme to be valid.
 */
if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
