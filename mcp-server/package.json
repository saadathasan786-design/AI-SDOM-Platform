{
  "name": "wp-mcp-server",
  "version": "1.0.0",
  "description": "MCP server that lets Claude control a WordPress site (local or live) via the WP REST API.",
  "type": "module",
  "private": true,
  "main": "index.js",
  "license": "MIT",
  "author": "AI-SDOM Contributors",
  "keywords": ["wordpress", "mcp", "model-context-protocol", "claude", "ai-sdom"],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.5.0",
    "dotenv": "^16.4.5",
    "node-fetch": "^3.3.2"
  }
}
